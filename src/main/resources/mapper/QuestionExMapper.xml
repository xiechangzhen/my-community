<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="life.showlin.community.mycommunity.mapper.QuestionExMapper">

  <update id="addViewCount" parameterType="life.showlin.community.mycommunity.model.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Fri Oct 04 11:43:07 CST 2019.
    -->
    update question
    SET view_count = view_count + 1
    where id = #{id,jdbcType=BIGINT}
  </update>

  <update id="addCommentCount" parameterType="life.showlin.community.mycommunity.model.Question">
    update question
    SET comment_count = comment_count +  1
    where id = #{id}
  </update>
    

  <select id="selectRelated" parameterType="life.showlin.community.mycommunity.model.Question" resultType="life.showlin.community.mycommunity.model.Question">
      SELECT * FROM question WHERE id != #{id} and tag regexp #{tag}
    </select>

  <select id="countBySearch" parameterType="life.showlin.community.mycommunity.dto.QuestionQueryDTO" resultType="java.lang.Integer">
    SELECT count(*) FROM question
    <where>
      <if test="search != null and search != ''">
        and title regexp #{search}
      </if>
    </where>
  </select>

  <select id="selectBySearch" parameterType="life.showlin.community.mycommunity.dto.QuestionQueryDTO" resultType="life.showlin.community.mycommunity.model.Question">
    SELECT * FROM question
    <where>
      <if test="search != null and search != ''">
        and title regexp #{search}
      </if>
    </where>
    order by gmt_create desc limit #{page} ,#{size}
  </select>

</mapper>